# Frontend password (embedded in JS bundle — acceptable for internal tool)
NEXT_PUBLIC_ACCESS_PASSWORD=

# Backend API key (must match API_KEY in backend/.env)
NEXT_PUBLIC_API_KEY=

# Backend URL (leave empty for default http://localhost:8002)
NEXT_PUBLIC_API_URL=
